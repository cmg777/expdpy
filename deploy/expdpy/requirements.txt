# Dependency set shinyapps.io installs on its build server.
# Install the released package from PyPI with the Shiny "app" extra (shiny, shinywidgets,
# nbformat, cryptography, openpyxl) and the "panel" extra (linearmodels) so the Panel-models
# card works. This also pulls the runtime deps (pandas/numpy/scipy/plotly/pyfixest/
# great-tables/statsmodels/pyarrow) plus the package code and bundled assets.
#
# shinyapps.io caches the env by this file's hash. Pin a version to force a clean rebuild on
# a new release, e.g.:
#   expdpy[app,panel]==0.2.0
# Or track the unreleased main branch instead:
#   expdpy[app,panel] @ git+https://github.com/cmg777/expdpy.git
expdpy[app,panel]
